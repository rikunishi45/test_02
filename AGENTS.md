# AGENTS.md — レビューの判定基準

コードレビューの判定基準の正本。**書く側（Claude Code）と読む側（Codex）が同じ基準を参照する**
ために1か所に置いてある。Codex は GitHub のコードレビューでこのファイルの「Code Review Rules」
節を読む。Claude Code は `AGENTS.md` を直接読まないので、`CLAUDE.md` の `@AGENTS.md` で import
している。

`CLAUDE.md` との境界：あちらは開発フローと壁の定義。ここは**レビューで何を見るか**だけを書く。
重複させない。

---

## このリポジトリの前提

レビューの優先順位はこの構造から決まる。

- 個人開発。**`src/` に人間のレビューは入らない**（保護パス外なので自動マージされる）
- CIのテストが `src/` に対する唯一の壁。そしてそのテストを書くのもAI
- したがって **「テストが実装を追認していないか」が最重要の関心**になる。実装とテストを
  同じモデルが書く以上、ここは別モデルでしか見えない

技術スタックは未定。言語固有の規約は決まった時点で `.claude/rules/` に置く（ここには書かない）。

---

## Code Review Rules

重大なものだけを指摘する。以下の順に重い。

### 1. テストが実装を追認していないか（最優先）

テストは仕様を検査するもので、実装をなぞるものではない。次を指摘する。

- 実装の分岐構造を写しただけで、**仕様上あるべき入力が検査されていない**
- 期待値が実装から逆算されている（実装が返した値をそのまま期待値に書いている）
- モックが実装の内部呼び出し順や非公開の構造に依存している
- 境界値・空・null/undefined・不正入力・エラー経路のいずれかが**まるごと欠けている**
- 常に真になるテスト（アサーションが無い、例外を握り潰す、`assert True` 相当）

### 2. 壁を弱める変更

保護パスの変更は人間のマージを要するが、**意図は差分の中で読めるべき**。次を指摘する。

- `run-tests.sh` のカバレッジ閾値の低下、計測対象の縮小
- CIに条件分岐が増え、特定の場合にテストを飛ばす経路ができている
- `.claude/settings.json` の `deny` の削除・緩和
- テストの skip / xfail / コメントアウト

### 3. 入力検証の位置

検証は**システム境界（ユーザー入力・外部API・ファイル読み込み）でのみ**行う。境界の内側に
散らばった防御的チェックと、境界に検証が無いまま内部を信頼している箇所の両方を指摘する。

### 4. 起こり得ないケースへのエラーハンドリング

到達不能な分岐、自分の内部呼び出しに対する try/catch、フレームワークが既に保証している条件の
再確認。

### 5. 秘密情報・認証情報

CIの gitleaks はパターンマッチ。**パターンに載らない漏洩**を見る。ログに混ざる資格情報、
URLのクエリに入る個人情報、内部構造を露出するエラーメッセージ。

### 6. 不可逆操作

削除・上書き・外部への送信が、確認も dry-run も無しに追加されていないか。

---

## 指摘しないこと

誤検知はレビューのループを空回りさせる。次は**指摘しない**。

| 対象 | 理由 |
|---|---|
| フォーマット、命名の好み | 判断が分かれるだけで、実行時のバグにならない |
| テストの成否、カバレッジ閾値、gitleaks の検出範囲 | CIが機械的に見ている。二重に言う意味がない |
| コメントの多寡 | 「なぜが自明でない場合のみ書く」を採用済み。足せという指摘はしない |
| `tests/` のコード品質そのもの | 見るのは「テストが実装を追認していないか」であって、テストの美しさではない |
| 抽象化の不足 | 最小限のコードを書く方針を採用済み。将来の拡張を理由にした抽象化は提案しない |

判断に迷う指摘は出さない。**確信の持てるものだけを、少なく出す。**
