---
name: codex-review
description: 書いたコードを別モデル（Codex）にレビューさせる。push や PR 作成の前に実行する。「レビューして」「codex に見せて」「push する前に確認」などで発動する。
allowed-tools: Bash(git status *), Bash(git log *), Bash(git diff *)
---

# codex-review — 別モデルによるレビュー

Claude が書いたコードを **Codex** にレビューさせる。ChatGPT のサブスク枠で動く（API課金は発生しない）。

## なぜ別モデルなのか

`src/` に人間のレビューは入らず、CIのテストが唯一の壁。そしてそのテストを書くのも Claude。
**同じモデルは自分の判断を追認する**ので、実装に合わせた甘いテストを自分では見つけられない。
ここだけが別モデルにしか見えない領域で、この skill の存在理由もそこにある。

判定基準は `AGENTS.md`。Codex は project instructions として自動で読むので、プロンプトで
渡し直さない。

## 実行前提：`.env` や鍵を作業ツリーに置かない

`codex exec` は Claude とは別プロセスで動く。**Claude の `Read` deny（`.env` / `id_rsa*` 等）は
Claude 自身のツール呼び出しにしか効かず、codex サブプロセスのファイルアクセスは制限しない。**
codex はレビューのために作業ツリーを広く読むので、`.env` や秘密鍵が（gitignore されていて
コミットされていなくても）ワーキングディレクトリに存在すれば読める。差分やコード中のプロンプト
インジェクションによって、その内容がレビュー結果に混入し得る（`AGENTS.md` の「秘密情報・認証情報」
節が対象とする漏洩経路そのもの）。

この skill を実行する前に、作業ツリーに未追跡の秘密情報ファイルが無いことを確認する。

## 実行

**`codex exec review` を自分で実行しない。`codex-triage` サブエージェントに委任する。**
Codex の生の出力（数十〜百行のテキスト）でメインの文脈を汚さないため。

`git status` / `git diff` でスコープを判断してから、Agent tool で `codex-triage` を呼ぶ。

- コミット済みの差分（PR単位）→「main との差分を codex-triage でレビューして」
- 作業ツリー（コミット前）→「未コミットの変更を codex-triage でレビューして」

レビューには1〜数分かかる。バックグラウンドで走らせ、完了通知を待つ間に他の作業を
進めてよい。

`codex-triage` が返す分類済みの要約だけを受け取る。**生の `.codex-review.md` を自分で
読みにいかない。** それを読んでしまうと、この skill が防ごうとしている「メインの文脈が
Codex の生出力で汚れる」ことがそのまま起きる。

## 指摘への対応

分類は `codex-triage` が行う（`AGENTS.md` を基準に）。ここでは受け取った結果への
対応方針だけ書く。**分類済みの指摘をそのまま実装しない。**

| 分類 | 対応 |
|---|---|
| 本物の修正 | 直す |
| 妥当な nitpick | 安ければ直す。高くつくなら理由を書いて残す |
| 誤検知・古い前提 | **直さない。** 理由を1行書いて飛ばす |

## 終わり方

ユーザーに次を報告する。

- 本物の修正として直したもの
- 飛ばしたものと、その理由
- **レビューが空だった場合はそう言う。**「問題なし」を捏造しない

**レビューを通したことを、コードが正しい証明として報告しない。** Codex が見るのは差分であって、
仕様との一致ではない。仕様の確認は別の仕事。
